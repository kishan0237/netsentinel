# Wheels exist for these minimum versions on every CPython Render offers
# (including 3.13/3.14), so pip never falls back to source builds.
fastapi>=0.115.6
uvicorn[standard]>=0.37.0
sqlalchemy>=2.0.44
psycopg[binary]>=3.2.10
pydantic>=2.12.0,<3
requests>=2.32.3
python-dotenv>=1.0.1
pytest>=8.3.4
httpx>=0.28.1
